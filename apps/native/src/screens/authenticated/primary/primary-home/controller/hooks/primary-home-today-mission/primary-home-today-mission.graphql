query Primary_HomeTodayMission($userId: String!) {
  todayMission(userId: $userId) {
    mission {
      category
      missionId
      questions {
        questionId
        questionOrder
        content
        responseType
      }
    }
    coupleMission {
      isCompleted
    }
    partnerResponse {
      isCompleted
    }
    userResponse {
      isCompleted
      data {
        questionId
        content
      }
    }
  }
}
