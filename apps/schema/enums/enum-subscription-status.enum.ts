export enum EnumSubscriptionStatus {
  'UNSUBSCRIBED' = 'UNSUBSCRIBED',
  'ACTIVE' = 'ACTIVE',
  'ACTIVE_TRIAL' = 'ACTIVE_TRIAL',
  'CANCELED' = 'CANCELED',
  'PAUSED' = 'PAUSED',
  'EXPIRED' = 'EXPIRED',
}
